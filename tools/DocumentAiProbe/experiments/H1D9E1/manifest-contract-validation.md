# Contrato completo del manifest

- architecture: `ERROR / CONTRACT_INVALID`; sesión creada: no; aprobado: True.
- preprocessing_version: `ERROR / CONTRACT_INVALID`; sesión creada: no; aprobado: True.
- class_mapping: `ERROR / CONTRACT_INVALID`; sesión creada: no; aprobado: True.
- threshold: `ERROR / CONTRACT_INVALID`; sesión creada: no; aprobado: True.
- external_data: `ERROR / CONTRACT_INVALID`; sesión creada: no; aprobado: True.

- Sesiones ONNX creadas durante corrupciones: 0.
- Modelo original modificado: no.
